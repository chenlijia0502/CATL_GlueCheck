1.以上两个文件夹（MESDATA,MESLOG），请存放本地路径 D:\

2.MESDATA中的数据文件，收集的参数数据保存在本地文件中，路径,内容,路径,文件名，内容,大小写格式等请严格按照示例（csv数据文件路径为D:\MESDATA\MES\SFC，
文件名为sfc_日期时间，内容格式如示例）

3.MESLOG中的日志，by天by接口，生成接口交互日志，路径,文件名，内容,大小写格式等请严格按照示例（MESLOG中的文件夹名为接口方法名，excel文件名为日期，
excel里面日志内容所在工作表名为Sheet1，一次接口调用log 7行，如果接口中没有sfc，为空即可，该行仍保留，两次接口调用的log内容之间空一行）

4.接口程序的配置ini文件，请命名为MESCFG.ini，文件路径不限定，每个方法定义一个section，section名以接口方法名定义，具体见示例
